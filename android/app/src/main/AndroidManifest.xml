<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Casting/segunda pantalla (feat/cast-second-screen): mDNS/DNS-SD + red local -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Android 13+ (targetSdk 36): descubrir/conectar dispositivos en la LAN
         (mDNS + sockets) sin geolocalización. Necesario para que el casting
         funcione en hardware real. -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- background_downloader (offline downloads): Android 13+ requires this
         to show the 'running'/'complete'/'error'/'paused' notifications
         configured in DownloadService._configureOnce. Without it, downloads
         still run but never promote to a foreground service and the OS is
         more likely to kill them once the app is backgrounded. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- EPG programme reminders: exact alarms fire the notification at the right
         time. USE_EXACT_ALARM is auto-granted for an alarm/reminder app;
         SCHEDULE_EXACT_ALARM is the fallback the OS may revoke, in which case the
         reminder degrades to an inexact alarm (see ReminderService). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Re-arm scheduled EPG reminders after a device reboot / app update.
         Without this, flutter_local_notifications' AlarmManager alarms are lost
         on reboot and the reminder never fires (the guide window is 2 days, so a
         restart in between would silently drop it). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- background_downloader (offline downloads): on Android 14+ (API 34+;
         this app targets API 36), the plugin calls setForeground(..., dataSync)
         while a download's 'running' notification is active (see its
         Notifications.kt), to keep the download alive as a foreground service.
         Android 14 requires the matching FOREGROUND_SERVICE_* permission for
         whichever type is used at runtime, even though the service itself
         (WorkManager's SystemForegroundService) is declared by the androidx.work
         library, not by us. NOTE: on API 35+ dataSync foreground services have a
         ~6h/24h runtime budget — a very large download could hit it; the plugin
         resumes from background so this is a soft limit, not data loss. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Pausar la TV al recibir una llamada mientras se castea: el móvil escucha
         el estado de la telefonía (TelephonyManager/TelephonyCallback) para
         pausar/reanudar la reproducción en la TV. Solo se registra el listener
         cuando la función está activa y el casting en curso; si el usuario no
         concede el permiso, la función es un no-op silencioso (nunca crashea). -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- In-app file browser (mainly for Android-TV boxes whose stripped
         DocumentsUI can't satisfy the system picker). Scoped to legacy
         storage; on API 33+ the system grants a no-op stub and the
         browser is limited to app-private dirs, which is by design. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Android TV / Leanback support: app appears in the TV launcher and
         declares that touchscreen/telephony are not required so it installs
         on TV devices and the Play Store doesn't filter it out. -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.faketouch"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <application
        android:label="Rensi IPTV"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:banner="@drawable/tv_banner"
        android:isGame="false"
        android:usesCleartextTraffic="true"
        android:allowBackup="false"
        android:fullBackupContent="false"
        tools:replace="android:allowBackup"><!-- Local DB (rensi-iptv.sqlite) may hold Xtream creds; keep it out of
             cloud Auto Backup. tools:replace ensures our value wins any
             library manifest merge. -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:windowSoftInputMode="adjustResize">

            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Audio Service -->
        <service android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true" tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <!-- Media Button Receiver -->
        <receiver android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true" tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- flutter_local_notifications: deliver scheduled EPG reminders and
             re-schedule the pending ones after a reboot / app replacement. -->
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"
            android:exported="false" />
        <receiver
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
    </application>

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ package visibility: without this, resolveActivity() for the
             voice-recognition overlay returns null even when a recognizer is
             installed, hiding the mic everywhere. See VoiceSearchService. -->
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH"/>
        </intent>
        <!-- Android 11+ package visibility: without this, canLaunchUrl()/launchUrl()
             (url_launcher) for https:// links (e.g. "Support on GitHub" in Settings)
             silently returns false and the tap does nothing. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https"/>
        </intent>
        <!-- "Open in external player" (VLC/MX/…): declare the VIEW+video/* intent
             so package visibility (Android 11+) lets the chooser resolve them;
             without this the intent silently fails to find any handler. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:mimeType="video/*"/>
        </intent>
    </queries>
</manifest>